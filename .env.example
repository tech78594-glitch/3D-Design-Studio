# This app no longer requires any AI backend, API key, or token.
# The AI Design Engine and Auto-Texture Generator features run entirely
# on local algorithmic/parametric and procedural-canvas generators —
# no network calls, no environment variables needed.
